# Scope boundary

V2 covers sporting lifecycle, action authorization, compatibility projection, audit and capture UI. Period engine, substitutions, scorer handoff and privileged sporting reopen remain separate future increments.